# fingen

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fingen` to build the library.

## Running unit tests

Run `nx test fingen` to execute the unit tests via [Jest](https://jestjs.io).
